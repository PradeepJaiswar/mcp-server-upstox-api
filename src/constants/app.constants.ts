/**
 * Application-level constants
 */

// Application info
export const APP_INFO = {
  NAME: 'mcp-service',
  VERSION: '1.0.0',
  DESCRIPTION: 'MCP Service CLI',
};

// Environment constants
export const ENVIRONMENTS = {
  DEVELOPMENT: 'development',
  PRODUCTION: 'production',
  TEST: 'test',
};

// CLI flags
export const CLI_FLAGS = {
  SERVER: '--server',
  SERVER_SHORT: '-s',
};